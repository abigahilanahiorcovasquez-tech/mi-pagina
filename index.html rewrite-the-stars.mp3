<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Para Miguel 💖</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: "Georgia", serif;
      background: linear-gradient(135deg, #00c853, #2196f3);
      overflow: hidden;
      color: white;
      text-align: center;
    }

    h1 {
      margin-top: 15%;
      font-size: 2.5em;
      text-shadow: 2px 2px 5px #000;
    }

    button {
      padding: 15px 30px;
      font-size: 1.2em;
      border: none;
      border-radius: 15px;
      background: #ff4081;
      color: white;
      cursor: pointer;
      box-shadow: 0 4px 15px rgba(0,0,0,0.3);
      transition: transform 0.2s;
      margin-top: 30px;
    }
    button:hover {
      transform: scale(1.1);
    }

    .frase {
      font-size: 1.5em;
      margin: 20px;
      opacity: 0;
      transition: opacity 1s;
    }

    .frase.show {
      opacity: 1;
    }

    .flotante {
      position: absolute;
      font-size: 2em;
      color: rgba(255,255,255,0.8);
      animation: flotar 7s linear infinite;
    }

    @keyframes flotar {
      from { transform: translateY(100vh); opacity: 1; }
      to { transform: translateY(-10vh); opacity: 0; }
    }
  </style>
</head>
<body>

  <h1>💖 Para Miguel 💖</h1>
  <button id="startBtn">Abrir sorpresa 🎁</button>

  <div id="contenido">
    <p class="frase">Y cuando vi tu sonrisa lo supe, esa era la sonrisa que quería ver siempre al despertar durante el resto de mi vida.</p>
    <p class="frase">Te quiero para volvernos locos de risa, ebrios de nada y pasear por las calles, eso sí, tomados de la mano, mejor dicho, del corazón.</p>
    <p class="frase">Cinco minutos bastan para que tu amor me haga sonreír como nunca.</p>
    <p class="frase">Cada instante contigo es un regalo que quiero guardar en mi corazón.</p>
    <p class="frase">Contigo, cada día es una aventura llena de risas y amor.</p>
  </div>

  <!-- Corazones y estrellas -->
  <span class="flotante" style="left:10%; animation-duration:6s;">❤️</span>
  <span class="flotante" style="left:30%; animation-duration:8s;">⭐</span>
  <span class="flotante" style="left:50%; animation-duration:7s;">💖</span>
  <span class="flotante" style="left:70%; animation-duration:9s;">❤️</span>
  <span class="flotante" style="left:90%; animation-duration:10s;">⭐</span>

  <!-- Música -->
  <audio id="musica" src="rewrite-the-stars.mp3"></audio>

  <script>
    const startBtn = document.getElementById('startBtn');
    const frases = document.querySelectorAll('.frase');
    const musica = document.getElementById('musica');

    // Ocultar frases al inicio
    frases.forEach(frase => frase.style.opacity = 0);

    // Acción al presionar el botón
    startBtn.addEventListener('click', () => {
      // Reproducir música
      musica.play();

      // Mostrar frases una a una
      frases.forEach((frase, i) => {
        setTimeout(() => {
          frase.classList.add('show');
        }, i * 3000); // cada frase aparece cada 3 segundos
      });

      // Ocultar botón
      startBtn.style.display = 'none';
    });
  </script>

</body>
</html>
